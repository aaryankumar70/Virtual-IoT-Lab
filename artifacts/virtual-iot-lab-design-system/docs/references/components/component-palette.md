# Component palette

- **Source:** `artifacts/virtual-iot-lab/src/App.tsx`, `Palette` lines 103–117.
- **Styles:** `.panel-header`, `.palette`, `.palette-group`, `.palette-item`, `.palette-icon`, `.example-card`.
- **Behavior:** groups registry-defined hardware by board/basic/actuator/sensor role; each row exposes a part label, technical descriptor, and add affordance.
- **Port note:** the preview uses representative registry items and keeps add callbacks out of the visual reference.